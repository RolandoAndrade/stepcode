---
'@stepcode/textmate': minor
---

First release: `generateGrammar(profile, { name, scopeName })` builds a TextMate grammar from any
resolved profile, and `stepcode`, `stepcodeEn`, `stepcodePseint` (also as `grammars/*.json`) are
ready for Shiki's `langs`.
